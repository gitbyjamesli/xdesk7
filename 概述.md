分析了 flutter 目录下的代码结构。这是 RustDesk 的现代 UI（替代旧的 Sciter UI），Windows / Linux 桌面版主要界面全部由它实现。下面按模块给出重点分析。

## 一、入口与启动方式

**main.dart** 是唯一入口，根据启动参数分发到不同窗口：

| 启动参数 | 进入的界面 |
|---|---|
| `multi_window <id> <json>` | 远程桌面 / 文件传输 / 摄像头 / 端口转发 / 终端 子窗口 |
| `--cm` | 连接管理窗口（接收方，看谁连过来） |
| `--install` | Windows 安装向导 |
| （无参数） | 主窗口（首页 + 设置） |

核心设计是**每种连接类型 = 一个独立子窗口**，每个子窗口内部再用"多标签页"框架管理多个会话。

## 二、桌面主窗口（pages）

- **desktop_tab_page.dart**：主窗口的标签壳，默认只有"首页"标签，右上角有"设置"按钮。
- **desktop_home_page.dart**（约 1175 行）：主界面主体，左右分栏：
  - 左栏：预设密码警告、Logo、**ID 面板**（双击复制、`...` 打开设置）、**密码面板**（一次性密码 + 刷新/编辑）、帮助卡片（更新/安装提示、macOS 权限、Linux SELinux/Wayland 提示）、插件入口（`PluginUiManager`）。
  - 右栏：`ConnectionPage()`——**按 ID 连接**面板（自动补全搜索所有设备）、连接按钮 + 右键菜单（文件传输 / 摄像头 / 终端）、在线服务状态指示。
  - 还负责注册多窗口事件处理器、每 1 秒轮询 ID/服务状态。
- **`connection_page.dart`**：右侧"输入 ID 连接"的核心面板。
- **desktop_setting_page.dart**（约 2940 行）：完整设置界面，左侧图标栏 + 右侧 8 个分页（`SettingsTabKey`）：
  - `general` 常规（主题/服务/音频输入/录制/壁纸/硬编/语言）
  - `safety` 安全（2FA、改 ID、权限、密码、白名单）
  - `network` 网络（服务器/中继设置）
  - `display` 显示（视图样式、滚动方式、画质、编解码器、隐私模式、边缘滚动厚度）
  - `account` 账号（OIDC 登录，用 `common/widgets/login.dart`）
  - `plugin` 插件
  - `printer` 打印机（仅 Windows）
  - `about` 关于
- **`install_page.dart`**：Windows 安装向导（路径选择、快捷方式、EULA、免安装运行）。
- **`server_page.dart`**：**连接管理器**（`--cm`，接收方界面）——客户端标签栏 + 主控制页 + 侧边面板（聊天 `ChatPage` / 文件传输日志），含未读角标、远程控制拦截、UAC 提权处理。

## 三、远程桌面会话（核心功能）

- **`remote_page.dart`**（约 1040 行）：单个远程会话页，持有一个 `FFI` 对象（连接 Rust 核心的"总对象"）和 `ImageModel`（视频帧）、`CursorModel`（光标）、`CanvasModel`（缩放/滚动）、`InputModel`（输入）、`TextureModel`（GPU 纹理渲染）、`RecordingModel`（录制）。
- **`remote_tab_page.dart`**：多标签远程窗口，标签页带连接类型图标（加密/直连）、指纹提示、未读聊天角标、右键菜单（移到新窗口、重启设备、Ctrl+Alt+Del、插入锁定、复制指纹等），以及相对鼠标模式提示。
- **remote_toolbar.dart**（约 2660 行）：浮动的远程控制工具栏，是远程界面的"指挥中心"，包含：
  - 状态管理 `ToolbarState`（pin 固定 / collapse 折叠 / hide 隐藏，从会话选项初始化）
  - 各菜单：**监视器选择**（含多显示器）、**控制**（鼠标/键盘/仅查看）、**显示**（自适应/原始/自定义缩放）、**分辨率**（含自定义分辨率）、**键盘模式**、**聊天**、**语音通话**、**录制**、**关闭**
  - 通用按钮组件 `MenuButton` / `CkbMenuButton` / `RdoMenuButton`
- **`view_camera_page.dart`**：摄像头会话页，与 remote_page 同构（`isViewCamera: true`，不转发键盘）。

## 四、文件传输 / 终端 / 端口转发

- **`file_manager_page.dart`**（约 1740 行）：**本地/远程双窗格**文件管理器——面包屑/路径栏、可排序列头、右键菜单、多选、拖拽上传、传输任务状态列表。底层由 `models/file_model.dart`（`FileController`、`JobController`）驱动。
- **`terminal_page.dart`**：封装 `xterm` 的 `TerminalView`，配合 `TerminalModel`；`terminal_connection_manager.dart` 维护"每个被控端一个 FFI"（tag 为 `terminal_$peerId`）。`terminal_tab_page.dart` 支持多会话恢复（`kOptionTerminalPersistent`）和快捷键。
- **`port_forward_page.dart`**：端口转发规则表格（本地端口 → 远端 host:port），支持增删规则，另有 **RDP 模式**。
- 这三个各有对应的 `*_tab_page.dart` 多标签壳。

## 五、多窗口标签框架（`desktop/widgets/`）

- **`tabbar_widget.dart`**（约 1540 行）：所有子窗口共用的标签体系——`TabInfo`、`DesktopTabController`（增删/跳转/保活）、`DesktopTab`（标签栏 + 页面视图 + 窗口操作按钮），含状态恢复（`RestorationMixin`）、自定义标题栏、TabbarTheme 亮暗主题。
- `titlebar_widget.dart`、`button.dart`、`dragable_divider.dart`、`refresh_wrapper.dart`、`update_progress.dart`、`kb_layout_type_chooser.dart`（ISO/ANSI 键盘布局选择）、`list_search_action_listener.dart`（列表打字即搜）。
- `material_mod_popup_menu.dart`（约 1430 行）：自实现的右键弹出菜单，全项目通用。

## 六、共享 UI（widgets）

- **`peers_view.dart` / `peer_card.dart`**：设备列表（最近 / 收藏 / 发现 / 通讯录 / 我的分组 5 类标签），卡片支持排序、多选、右键菜单、通讯录标签绘制。
- **`peer_tab_page.dart`**：设备列表上方的可拖拽标签切换栏。
- **`address_book.dart`**：通讯录（分组下拉、标签面板、权限处理）。
- **`login.dart`**：OIDC 登录界面。**`my_group.dart`**：自定义服务器分组。
- **`chat_page.dart`**：聊天界面（连接管理器 + 内嵌聊天两种模式）。
- **`dialog.dart`**（约 2800 行）：**对话框库**——密码强度校验、2FA、UAC、权限、中继提示、等待图像、可信设备等几十种对话框。
- **`overlay.dart`**：浮层基础设施（可拖拽聊天窗、`QualityMonitor` 网络质量浮层、`BlockableOverlay` 阻塞遮罩）。
- **`remote_input.dart`**：桌面/移动端共享的输入管道（原始按键焦点、触摸转鼠标事件、相对鼠标）。

## 七、状态层（models）

- **model.dart**（约 4036 行）：核心。`FFI` 是每个会话的"总对象"——`start()`/`close()`、事件路由（`startEventListener` 处理几十种核心事件：权限、光标、剪贴板、聊天、文件、录制、截图、打印机等），并持有全部子模型。`FfiModel` 管理对端信息/权限/显示器；`ImageModel` 解码视频帧；`CanvasModel` 负责缩放/滚动/边缘滚动；`CursorModel` 处理自定义光标。
- **`platform_model.dart`**（约 1500 行）：`bind` 对象——自动生成的到 Rust 核心的**静态桥接层**，暴露所有 `bind.mainXxx` / `bind.sessionXxx` 调用。
- 其他：`input_model.dart`（指针→Rust、键盘模式、相对鼠标）、`file_model.dart`、`terminal_model.dart`、`chat_model.dart`、`server_model.dart`、`ab_model.dart`（通讯录）、`user_model.dart`、`relative_mouse_model.dart`（指针锁定模式）、`state_model.dart`（全局单例状态）。

## 八、桥接架构（native 与 `plugin/`）

- **`native/`**：平台条件编译垫片——`win32.dart` 用 Dart FFI 直接调 `ntdll.dll` 的 `RtlGetVersion` 获取 Windows 版本号；`custom_cursor.dart` 在系统层注册自定义光标。
- **`plugin/`**：RustDesk 插件框架——`PluginManager` 解析插件列表、`PluginUiManager` 在首页注册插件入口、`widgets/desktop_settings.dart` 把插件设置渲染进"设置→插件"页。
- **`utils/multi_window_manager.dart`**：跨窗口通信核心（通过 `kWindowEvent*` 字符串方法调用），负责打开/激活子窗口、标签跨窗口移动、`openMonitorSession` 等。

## 九、移动端（简述）

pages：`home_page.dart`（底部导航）、`connection_page.dart`、`remote_page.dart`（触控手势 + 悬浮鼠标 + 软键盘输入）、`server_page.dart`（接收端）、`settings_page.dart`、`file_manager_page.dart`、`scan_page.dart`（扫码导入服务器配置）。与桌面共用 models，界面各自实现。

## 关键架构结论

1. **一个会话一个 `FFI` 对象**（`models/model.dart` 里的"上帝对象"），通过 `bind`（`platform_model.dart`）桥接 Rust 核心，事件经 `startEventListener` 路由到各子模型。
2. **每种连接类型 = 独立子窗口 + 多标签壳**：统一模式为 `*Screen → *TabPage → *Page`，全部复用 `DesktopTabController`/`DesktopTab`。
3. **远程画面渲染三件套**：`ImagePaint`（视频帧）+ `CursorPaint`（自定义光标）+ `QualityMonitor`（网络质量），由 `CanvasModel` 统一协调缩放/滚动，可选 GPU `Texture` 渲染（`TextureModel`）。
